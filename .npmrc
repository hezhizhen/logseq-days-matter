# Pin the public npm registry for this project so installs never inherit a
# private/internal registry from a user-global ~/.npmrc. This keeps
# package-lock.json portable: resolved URLs stay on registry.npmjs.org, so CI
# (and anyone outside an internal network) can `npm ci` successfully.
registry=https://registry.npmjs.org/
